{
  "name": "hello-jenkins",
  "version": "1.0.0",
  "description": "A simple Node.js project to test Jenkins pipelines",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --ci --reporters=default --reporters=jest-junit"
  },
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0"
  },
  "jest-junit": {
    "outputDirectory": ".",
    "outputName": "jest-junit.xml"
  }
}

